# realtime-systems

Small C++23 playground for real-time systems coursework.

## Build and Run

```sh
./run.sh
```

The CMake setup exports compile commands into `build/compile_commands.json`; the
repo-local `.clangd` points clangd there for Neovim.
